[project]
name = "parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gustav Leth-Espensen", email = "Gustavle@hotmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "coverage>=7.16.1",
]

[project.scripts]
parser = "parser:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "parcer",
]
